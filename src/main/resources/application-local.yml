spring:
  config:
    activate:
      on-profile: local
  jpa:
    show-sql: true

logging:
  level:
    root: INFO
    com.gymcheck: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
